let g:equinusocio_material_style = 'default'

" less bright
" which means some colors will be modified by this formula:
" (r, g, b) -> ( max(r - less, 0), max(g - less, 0), max(b - less, 0) )
" let g:equinusocio_material_less = 35
