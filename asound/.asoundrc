pcm.usb
{
    type hw
    card Snowball
}

pcm.intel
{
  type hw
  card Generic
}

pcm.!default
{
    type asym
    playback.pcm
    {
        type plug
        slave.pcm "intel"
    }
    capture.pcm
    {
        type plug
        slave.pcm "usb"
    }
}
